/** Subdirectory basename for compare payloads under the project root (`jest.config` rootDir). Safe to import without loading matchers or `jest-canvas-mock`. */
export const DEFAULT_COMPARE_PAYLOAD_DIRECTORY = '.jest-canvas-mock-compare';
